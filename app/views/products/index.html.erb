<h1>Main Page</h1>

<% @products.each do |product| %>
	<p><%= link_to product.name, product_path(product)%> |   <%= product.description%> <%= product.formatted_price %>$ </p>
	<% end %>

	<%= link_to "Add a product", new_product_path %>