<h1>All Users</h1>

<% @user.each do |user| %>
	<ul>
	<li> <%= user.name %> <%= link_to "Delete", user_path(user), method: :delete, data: {confirm: "Are you sure you want to delete"} %> </li>
	</ul>
	<% end %>
